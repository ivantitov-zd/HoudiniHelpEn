#type: node
#context: vop
#internal: osl_texture

= OSL Texture Map =

Performs a texture lookup.

See the OSL `texture()` function in the OSL documentation.

@related
    - [Node:vop/texture]
    - [Node:vop/rsl_texture]
