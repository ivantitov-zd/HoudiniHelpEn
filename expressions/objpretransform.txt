#type: expression
#category: objects

= objpretransform =

"""Returns an object's pre-transform matrix."""

:usage: `objpretransform(<<object_name>>)`

@examples
    {{{
    #!exp
    objpretransform("/obj/bone1")
    }}}
    
    Returns the pretransform matrix for /obj/bone1.
