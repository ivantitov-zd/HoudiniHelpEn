#type: node
#context: shop
#internal: rsl_vopimager
#icon: SHOP/rsl_vopimager

= VOP RSL Imager =

"""Container for an RSL imager shader implementation."""

This SHOP provides an container for a VOP implementation of an RSL
imager shader.

@parameters

Compiler:
    Specifies the command line used to compile the RSL code.
Force Compile:
    Compiles the RSL code.
