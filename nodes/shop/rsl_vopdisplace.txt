#type: node
#context: shop
#internal: rsl_vopdisplace
#icon: SHOP/rsl_vopdisplace

= VOP RSL Displacement =

"""Container for an RSL displacement shader implementation."""

This SHOP provides an container for a VOP implementation of an RSL
displacement shader.

@parameters

Compiler:
    Specifies the command line used to compile the RSL code.
Force Compile:
    Compiles the RSL code.
