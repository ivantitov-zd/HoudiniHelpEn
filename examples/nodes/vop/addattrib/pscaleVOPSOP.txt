I attached a file that shows various ways to create 
the pscale attribute. It uses three key nodes:
1) Property POP to add pscale
2) Point SOP to add pscale
3) a VOP SOP to add pscale (that is tricked out to also do random offsets)