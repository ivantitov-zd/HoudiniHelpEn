#type: hscript

= rps =

"""Lists background render processes."""

:usage: `rps`

The command lists the PID of the process, the host which the command is
running on, the command being run, and the frame being rendered.
Suspended processes are marked.



@related
    - [rkill|Cmd:rkill]

