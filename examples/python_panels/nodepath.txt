= Node path =

"""How to listen for changes to the current node path."""

In this example, the `onNodePathChanged(node)` hook is implemented in the
Python Panel code in order to listen for changes to the current node path.

Select different nodes, jump in and jump out of nodes to see the Python Panel
follow the current node.

:source:
    #path: nodepath.pypanel
    #lang: python

