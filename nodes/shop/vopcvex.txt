#type: node
#context: shop
#internal: vopcvex
#icon: SHOP/vopcvex

= VOP CVEX =

"""Container for a CVEX shader implementation."""

This SHOP provides an container for a VOP implementation of a CVEX shader.

@parameters

Compiler:
    Specifies the command line used to compile the VEX code.
Force Compile:
    Compiles the VEX code.
