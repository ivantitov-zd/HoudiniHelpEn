= frac =

#type: vex
#context: all
#tags: math, number
#group: math

"""Returns the fractional component of the floating point number."""

:usage: `float frac(float n)`
:usage: `<vector> frac(<vector> v)`
    Returns the fractional component of the floating point number. For example, `frac(1.5)` would return `0.5`. For vectors, this done per-component.


