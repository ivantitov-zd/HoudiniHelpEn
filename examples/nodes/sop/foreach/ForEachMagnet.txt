This example uses the ForEach SOP in all three modes (by group, by attribute 
and by iteration) for you to study and use.
You can make any SOP that doesn't support local variables 
(like Magnet, for example) behave like one that does using any of the 
three methods shown here.
