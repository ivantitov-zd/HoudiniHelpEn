#type: node
#context: shop
#internal: rsl_voplight
#icon: SHOP/rsl_voplight

= VOP RSL Light =

"""Container for an RSL light shader implementation."""

This SHOP provides an container for a VOP implementation of an RSL light
shader.

@parameters

Compiler:
    Specifies the command line used to compile the RSL code.
Force Compile:
    Compiles the RSL code.
