= setpackedtransform =

#type: vex
#context: all
#tags: transform
#group: transform

"""Sets the transform of a packed primitive."""

:usage: `void setpackedtransform(int input, int primnum, matrix transform)`

Sets the transform of a packed primitive. This modifies the `P` attribute of
the primitive's point as well as its intrinsic `transform`.

@examples

{{{
#!vex
// matrix to transform by
matrix transform = ident();
rotate(transform, radians(45), {0,1,0});
translate(transform, {0,1,0});

matrix transform = getpackedtransform(0, @primnum);
setpackedtransform(0, @primnum, transform * tf);
}}}

@related
- [Vex:getpackedtransform]
- [Vex:packedtransform]

