#type: hscript

= takeautomode =

"""Sets the take auto-include mode."""

:usage: `takeautomode on|off`

The auto-include mode allows the setting of parameters and flags without
having to first manually include them into the active take. If no
options are given the current state is returned.



@related
    - [takeinclude|Cmd:takeinclude]

