This example demonstrates the use of a volume emitter to fill a
container with fluid.  The volume of the inside of a tank is
specified as volume emission geometry, and particles are emitted
randomly at points inside of this geometry for a specified number
of frames. This example uses an SPH fluid.
