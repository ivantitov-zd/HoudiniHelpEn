#type: node
#context: shop
#internal: rsl_vopvolume
#icon: SHOP/rsl_vopvolume

= VOP RSL Volume =

"""Container for an RSL volume shader implementation."""

This SHOP provides an container for a VOP implementation of an RSL volume
shader.

@parameters

Compiler:
    Specifies the command line used to compile the RSL code.
Force Compile:
    Compiles the RSL code.
