#type: node
#context: vop
#internal: metastart
#icon: VOP/metastart

= Meta-Loop Start =

"""Opens a geometry file and
initializes the handle to iterate through all metaballs at the position
specified."""

This operator opens a geometry file (which should contain metaballs) and
initializes the handle to iterate through all metaballs at the position
specified.



@related
    - [Node:vop/metanext]
    - [Node:vop/metaimport]

