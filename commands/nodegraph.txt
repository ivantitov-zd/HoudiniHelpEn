#type: hscript

= nodegraph =

"""This command is no longer used."""

This command was used to configure the old network editor pane. See
[networkeditor|Cmd:networkeditor] for information about configuring the
new (Houdini 16.0 and above) network editor pane.

